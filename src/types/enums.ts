export enum Paths {
  Home = '/',
  Register = '/register',
  Login = '/login',
  NewList = '/new-list',
  MyLists = '/my-lists',
  ExistingList = '/existing-list',
  DrawVolunteer = '/draw-volunteer',
  NotFound = '/404',
}

export enum RouteConditions {
  loggedIn = 'loggedIn',
  loggedOut = 'loggedOut',
  noCondition = 'noCondition',
}
